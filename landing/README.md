# reposizer landing site

Standalone Vite site for package landing and docs snippets.

## Local development

```bash
cd landing
npm install
npm run dev
```

## Production build

```bash
npm run build
npm run preview
```

Deploy the generated `dist/` directory to any static host.
